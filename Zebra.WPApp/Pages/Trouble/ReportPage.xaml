<phone:PhoneApplicationPage
    x:Class="Zebra.WPApp.Pages.Trouble.ReportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tools="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding TargetNullValue=Report_trouble}" Margin="12" Style="{StaticResource stlTituloPagina}" />
        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentReport" Grid.Row="1" Margin="12">
            <StackPanel>
                <TextBlock Text="{Binding TargetNullValue=Choose_one}"></TextBlock>
                <tools:ListPicker Name="lspTroubleCategory">
                    <tools:ListPicker.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"></TextBlock>
                        </DataTemplate>
                    </tools:ListPicker.ItemTemplate>
                </tools:ListPicker>
                <TextBlock Text="{Binding TargetNullValue=What_is_going_on}"></TextBlock>
                <TextBox Name="txtDescription"></TextBox>
            </StackPanel>
        </Grid>
    </Grid>
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar>
            <shell:ApplicationBarIconButton Text="Report" IconUri="/Assets/AppBar/upload.png" Click="btnReportClick"></shell:ApplicationBarIconButton>
            <shell:ApplicationBarIconButton Text="Share" IconUri="/Assets/AppBar/share.png" Click="btnShareClick"></shell:ApplicationBarIconButton>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>