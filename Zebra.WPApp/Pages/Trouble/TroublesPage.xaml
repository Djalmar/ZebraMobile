<phone:PhoneApplicationPage
    x:Class="Zebra.WPApp.Pages.Trouble.TroublesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="clr-namespace:Microsoft.Phone.Maps.Controls;assembly=Microsoft.Phone.Maps"
    xmlns:zebraLib="clr-namespace:ZebrasLib.Classes;assembly=ZebrasLib"
    xmlns:collections="clr-namespace:System.Collections.Generic;assembly=System.Collections"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">
    
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.Resources>
        </Grid.Resources>
        <phone:Panorama Title="{Binding TargetNullValue=wht_s_gng_n}">
            <phone:PanoramaItem Header="{Binding TargetNullValue=Mp}">
                <Grid>
                    <maps:Map Name="mapTroubles">
                    </maps:Map>
                </Grid>
            </phone:PanoramaItem>
            <phone:PanoramaItem Header="{Binding TargetNullValue=Troubles}">
                <ListBox>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Image Height="50" Width="50"></Image>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding title}"></TextBlock>
                                    <TextBlock Text="{Binding TargetNullValue=something}"></TextBlock>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </phone:PanoramaItem>
        </phone:Panorama>
        
    </Grid>

</phone:PhoneApplicationPage>